# This file demonstrates a config that is broken because
# The first object definition is missing a '}'

define command{
       command_name     check_mysql
       command_line     $USER1$/check_dummy.sh 2



define command{
        command_name    check_dummy
        command_line    $USER1$/check_dummy.sh $ARG1$
        #poller_tag     DMZ
}