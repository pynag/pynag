Any dataset with the name testdata* has a hardcoded file expected_results and if any modifications
are made to the configuration files here below the unit tests will fail.


The purpose of this is to detect if any subtle changes in the parsing algorithms are producing new bugs.
